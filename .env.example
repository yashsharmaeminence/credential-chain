# API origin when the Vite dev server and API run on different hosts/ports
# VITE_API_BASE_URL=http://127.0.0.1:3001

# Same value as apps/api INSTITUTION_API_SECRET when the API requires institution auth.
# Never ship this in a public-facing production site without understanding it is visible in the bundle.
# VITE_INSTITUTION_API_KEY=
